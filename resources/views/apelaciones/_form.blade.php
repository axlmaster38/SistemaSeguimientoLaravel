@csrf
<div class="row g-3">
    <div class="col-12"><label class="form-label" for="proceso_disciplinario_id">Proceso disciplinario</label><select id="proceso_disciplinario_id" name="proceso_disciplinario_id" class="form-select @error('proceso_disciplinario_id') is-invalid @enderror" required><option value="">Seleccione</option>@foreach ($procesosActivos as $proceso)<option value="{{ $proceso->id }}" @selected((int) old('proceso_disciplinario_id', $apelacion->proceso_disciplinario_id) === $proceso->id)>#{{ $proceso->id }} - {{ $proceso->denuncia?->estudiante?->codigo_estu }} - {{ $proceso->denuncia?->estudiante?->nombre }} {{ $proceso->denuncia?->estudiante?->apellido }}</option>@endforeach</select>@error('proceso_disciplinario_id')<div class="invalid-feedback">{{ $message }}</div>@enderror</div>
    <div class="col-12"><label class="form-label" for="tipo_apelacion">Tipo apelacion</label><select id="tipo_apelacion" name="tipo_apelacion" class="form-select @error('tipo_apelacion') is-invalid @enderror" required><option value="">Seleccione</option>@foreach ($tiposApelacion as $tipo)<option value="{{ $tipo }}" @selected(old('tipo_apelacion', $apelacion->tipo_apelacion) === $tipo)>{{ $tipo }}</option>@endforeach</select>@error('tipo_apelacion')<div class="invalid-feedback">{{ $message }}</div>@enderror</div>
    <div class="col-12"><label class="form-label" for="motivo">Motivo</label><textarea id="motivo" name="motivo" rows="5" class="form-control @error('motivo') is-invalid @enderror">{{ old('motivo', $apelacion->motivo) }}</textarea>@error('motivo')<div class="invalid-feedback">{{ $message }}</div>@enderror</div>
</div>
<div class="d-flex gap-2 mt-4"><button class="btn btn-primary" type="submit">Guardar</button><a href="{{ route('apelaciones.index') }}" class="btn btn-outline-secondary">Volver</a></div>
